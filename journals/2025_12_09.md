- #python
	- #### Списки
	- list = [] - создание пустого списка
	- artists = ['banksy', 'malevich', 'warhall'] - создание списка с элементами
	- print(artists[0]. title()) - обращение к элементу списка
	- artists.append('rotko') - добавление элемента в конец списка
	- artists.insert(1, 'muha') - добавление элемента в позицию 1
	- del artists[4] - удаление элемента с позиции 4
	- popped_artist = artists.pop() - удаление последнего элемента из списка и сохранение его в переменную
	- favorite_artist = artists.pop(2) - удаление элемента с позиции 2 и сохранение его в переменную
	- artists.remove('rotko') - удаляет элемент по значению
		- удаляет только первое вхождение заданного значения
	- artists.sort() - сортировка списка в алфавитном порядке
	- print(sorted(artists)) - временная сортировка списка, не меняет фактическое расположение элементов в самом списке
	- artists.sort(reverse=True) - сортирует список в обратном алфавитном порядке
	- artists.reverse() - размещает элементы списка в обратном порядке
	- len(artists) - определение длины списка
	- artists[-1] - обращение к последнему элементу списка
	- artists[-2] - к предпоследнему
	- artists[-3] - к третьему с конца
	- #### Циклы в работе со списками
	- for artist in artists:
	        print(f"{artist.title()} is a great artist
	-